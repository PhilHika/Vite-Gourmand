services:
  ###> doctrine/doctrine-bundle ###
  db:
    ports:
      - "5433:5432"
  ###< doctrine/doctrine-bundle ###

  # NOTE : le service `mailer` (Mailpit) a été retiré volontairement.
  # En dev hybride, le mailer utilisé est MailHog.exe standalone (host Windows,
  # ports 1025/8025). Garder un service `mailer` ici fait écraser MAILER_DSN par
  # `symfony serve` (auto-détection des services Docker) et casse l'envoi.
  # La migration vers Mailpit est planifiée — voir doc/mémoire dédiée.
