{% extends 'base.html.twig' %}

{% block title %}Liste des menus{% endblock %}

{% block body %}
    {# Point de montage Vue : MenuIndex.vue prend le relais ici #}
    <div id="app"></div>

    {# Modale de connexion : reste en HTML statique, ouverte programmatiquement par Vue
       (méthode ouvrirModaleLogin() de MenuIndex.vue). Vue lit/écrit dans #modalMessage
       et #modalLoginLink pour personnaliser le contenu selon le menu sélectionné. #}
    {% if not app.user %}
        <div class="modal fade" id="loginModal" tabindex="-1" aria-labelledby="loginModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header bg-primary text-white">
                        <h5 class="modal-title" id="loginModalLabel">
                            <i class="bi bi-lock"></i>
                            Connexion requise
                        </h5>
                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Fermer"></button>
                    </div>
                    <div class="modal-body text-center py-4">
                        <i class="bi bi-person-circle display-1 text-primary mb-3"></i>
                        <p class="lead" id="modalMessage">Pour passer une commande, veuillez vous connecter ou créer un compte.</p>
                    </div>
                    <div class="modal-footer justify-content-center">
                        <a id="modalLoginLink" href="{{ path('app_login', {_target_path: path('app_commande_new')}) }}" class="btn btn-primary btn-lg">
                            <i class="bi bi-box-arrow-in-right"></i>
                            Se connecter
                        </a>
                        <a href="{{ path('app_register') }}" class="btn btn-outline-secondary btn-lg">
                            <i class="bi bi-person-plus"></i>
                            Créer un compte
                        </a>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    {# Bundle Vue : ordre important — vendors (Vue + Bootstrap) AVANT notre code #}
    <script src="{{ asset('chunk-vendors.js', 'vue_build') }}" defer></script>
    <script src="{{ asset('menu-index.js', 'vue_build') }}" defer></script>
{% endblock %}
